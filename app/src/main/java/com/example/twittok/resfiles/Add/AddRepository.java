/*
package com.example.twittok.UI.Add;

import android.util.Log;

import com.example.twittok.Constants;
import com.example.twittok.Network.ApiObjects.Input.TwokToAdd;
import com.example.twittok.Network.CommunicationController;
import com.example.twittok.UI.ListenerInterfaces.OnResponse;

public class AddRepository {
    public static void addTwok(TwokToAdd twokToAdd, OnResponse onResponse) {
        CommunicationController.addTwok(
                twokToAdd,
                onResponse::giveOutput,
                t -> Log.d(Constants.ONFAIL, "addTwok call in method addTwok of class AddRepository: " + t.getLocalizedMessage())
        );
    }
}
*/
