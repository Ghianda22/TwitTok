package com.example.twittok.listeners.loaded;

import com.example.twittok.datasource.model.IsFollowed;

public interface OnIsFollowedLoadedListener {
    void isFollowedLoaded(IsFollowed isFollowed);
}
